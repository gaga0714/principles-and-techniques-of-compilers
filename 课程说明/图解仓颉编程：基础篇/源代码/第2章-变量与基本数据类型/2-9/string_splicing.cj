/*
 * 作者：刘玥 张荣超
 * 抖音/微信视频号/微信公众号：九丘教育
 */

main() {
    var r: Float64 = 1.6  // 圆的半径为1.6
    var info: String
    info = 
        "半径：" + r.toString() + " 圆周率：" + 3.14.toString() + 
        " 面积：" + (3.14 * r * r).toString()
    println(info)
}
